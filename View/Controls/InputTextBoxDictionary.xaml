<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:ctrl="clr-namespace:精密切割系统.View.Controls">
    <Style x:Key="InputTextStyle" TargetType="{x:Type ctrl:InputTextBox}">
        <!--  StyleFocusVisual在上一篇里说了  -->
        <!--<Setter Property="FocusVisualStyle" Value="{StaticResource StyleFocusVisual}" />-->
        <Setter Property="BorderBrush" Value="Silver" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type ctrl:InputTextBox}">
                    <Border
                        Name="brdText"
                        Padding="2"
                        Background="{TemplateBinding Background}"
                        BorderBrush="{TemplateBinding BorderBrush}"
                        BorderThickness="{TemplateBinding BorderThickness}"
                        SnapsToDevicePixels="true">
                        <Grid>
                            <ScrollViewer x:Name="PART_ContentHost" SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}" />
                            <StackPanel
                                Name="stpWatermark"
                                Orientation="Horizontal"
                                Visibility="Collapsed">
                                <TextBlock
                                    HorizontalAlignment="Left"
                                    VerticalAlignment="Center"
                                    Cursor="IBeam"
                                    FontFamily="{TemplateBinding FontFamily}"
                                    FontSize="{TemplateBinding FontSize}"
                                    Foreground="{Binding XWmkForeground, RelativeSource={RelativeSource TemplatedParent}}"
                                    Text="{Binding XWmkText, RelativeSource={RelativeSource TemplatedParent}}" />
                            </StackPanel>
                            <ContentPresenter />
                        </Grid>
                    </Border>
                    <ControlTemplate.Triggers>
                        <!--  当失去焦点并且没有输入任何内容时  -->
                        <MultiTrigger>
                            <MultiTrigger.Conditions>
                                <Condition Property="Text" Value="" />
                                <Condition Property="IsFocused" Value="False" />
                            </MultiTrigger.Conditions>
                            <MultiTrigger.Setters>
                                <Setter TargetName="stpWatermark" Property="Visibility" Value="Visible" />
                            </MultiTrigger.Setters>
                        </MultiTrigger>
                        <!--  当验证失败时  -->
                        <Trigger Property="XIsError" Value="true">
                            <!--  Value="#FFFF4545" MaterialSnackUtils new SolidColorBrush(Color.FromRgb(255, 69, 69));  -->
                            <Setter TargetName="brdText" Property="BorderBrush" Value="#FFFF4545" />
                            <!--<Setter TargetName="brdText" Property="Background" Value="Beige" />-->
                            <Setter Property="Foreground" Value="#FFFF4545" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
</ResourceDictionary>