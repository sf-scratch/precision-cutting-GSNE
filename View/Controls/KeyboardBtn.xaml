<UserControl x:Class="精密切割系统.View.Controls.KeyboardBtn"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:精密切割系统.View.Controls"
             mc:Ignorable="d" 
             Loaded="keyboardBtn_Loaded"
             d:DesignHeight="62" d:DesignWidth="72">
    <UserControl.Resources>
        <Style TargetType="Button">
            <Setter Property="BorderBrush" Value="{x:Null}"></Setter>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderBrush" Value="Transparent"/>

            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                BorderBrush="{TemplateBinding BorderBrush}" 
                BorderThickness="{TemplateBinding BorderThickness}">
                            <ContentPresenter />
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>
    <Border Background="#87B6D3" Focusable="False" x:Name="keyboardBorderBtn">
        <Button FontWeight="Bold" Foreground="#FFFFFF" x:Name="keyboardBtn" Focusable="False">
            <Button.Content>
                <TextBlock x:Name="keyboardText" Text="SDF" TextAlignment="Left" TextWrapping="Wrap" Padding="3,3,0,0"/>
            </Button.Content>
        </Button>
        <Border.Effect>
            <DropShadowEffect 
        ShadowDepth="5" 
        BlurRadius="20" 
        Color="#4DC9EBF7" 
        Direction="315" />
        </Border.Effect>
        <!-- 设置按钮内容的偏移效果 -->
        <Border.RenderTransform>
            <TranslateTransform x:Name="ContentTransform" X="0" Y="0" />
        </Border.RenderTransform>
    </Border>

</UserControl>
