<UserControl x:Class="精密切割系统.View.Pages.Auto.AutoCutSelectConfig"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:精密切割系统.View.Pages.Auto"
             xmlns:Controls="clr-namespace:精密切割系统.View.Controls" 
             xmlns:viewModel="clr-namespace:精密切割系统.ViewModel"
             xmlns:auto="clr-namespace:WpfAutoGrid;assembly=WpfAutoGrid.Core"
             xmlns:behaviors="http://schemas.microsoft.com/xaml/behaviors"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d" 
             d:DesignHeight="676" d:DesignWidth="1448"
             d:DataContext="{d:DesignInstance viewModel:AutoCutSelectConfigViewModel, IsDesignTimeCreatable=True}">
    <Grid >
        <Grid.Resources>
            <ResourceDictionary Source="/Themes/MaterialDesignResources.xaml"/>
        </Grid.Resources>
        <Grid.RowDefinitions >
            <RowDefinition Height="80"/>
            <RowDefinition Height="50"/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition />
        </Grid.ColumnDefinitions>
        <Label x:Name="lbMdParamList" Content="自动切割参数清单" VerticalAlignment="Center" HorizontalAlignment="Left" Grid.Row="0" Grid.Column="0"  FontWeight="Bold" FontSize="30" Margin="30,30,0,0"/>
        <Grid Grid.Row="1" Grid.Column="0" Margin="30,10,30,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <StackPanel Orientation="Horizontal" VerticalAlignment="Center" >
                <Label Content="当前选择参数号:" FontWeight="Bold" VerticalAlignment="Bottom" FontSize="16"/>
                <Label Content="{Binding CurrentSelectedConfigId}" VerticalAlignment="Bottom" FontSize="16"/>
                <Label Content="描述:" FontWeight="Bold" VerticalAlignment="Bottom" FontSize="16" Margin="15,0,0,0"/>
                <Label d:Content="参数AAA" Content="{Binding Describe}" FontSize="16" VerticalAlignment="Bottom"/>
            </StackPanel>
            <StackPanel Grid.Column="1" HorizontalAlignment="Right" >
                <Controls:InputTextBox Width="190" Height="35" InputType="Numeral" XMin="0" XPrecision="0" XWmkText="输入参数号可快捷操作" />
            </StackPanel>
        </Grid>
        <ListView Grid.Row="2" Grid.Column="0" d:ItemsSource="{d:SampleData}" ItemsSource="{Binding AutoCutConfigIdList}" SelectedItem="{Binding SelectedParamsConfig}" Margin="30,5,30,20">
            <ListView.ItemContainerStyle>
                <Style TargetType="{x:Type ListViewItem}">
                    <Setter Property="Height" Value="50"/>
                    <Setter Property="BorderThickness" Value="0"/>
                    <Style.Triggers>
                        <Trigger Property="IsMouseOver" Value="true">
                            <Setter Property="Background" Value="Transparent"/>
                        </Trigger>
                        <Trigger Property="IsSelected" Value="true">
                            <Setter Property="Background" Value="#CBE5ED"/>
                        </Trigger>
                    </Style.Triggers>
                </Style>
            </ListView.ItemContainerStyle>
            <ListView.ItemTemplate>
                <DataTemplate>
                    <StackPanel Orientation="Horizontal" Margin="10,0,0,0">
                        <Label Content="{Binding Id}" FontSize="18"  VerticalAlignment="Center" Width="100"/>
                        <Label Content="{Binding Describe}" FontSize="18"  VerticalAlignment="Center"/>
                    </StackPanel>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </Grid>
</UserControl>
