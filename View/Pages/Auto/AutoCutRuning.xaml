<UserControl x:Class="精密切割系统.View.Pages.Auto.AutoCutRuning"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:精密切割系统.View.Pages.Auto"
      xmlns:viewModel="clr-namespace:精密切割系统.ViewModel"
      xmlns:custom="clr-namespace:精密切割系统.CustomControls"
      xmlns:auto="clr-namespace:WpfAutoGrid;assembly=WpfAutoGrid.Core" 
      xmlns:Controls="clr-namespace:精密切割系统.View.Controls"
      xmlns:common="clr-namespace:精密切割系统.View.Pages.common"
      xmlns:behaviors="http://schemas.microsoft.com/xaml/behaviors"
      xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
      mc:Ignorable="d" 
      d:DesignHeight="676" d:DesignWidth="1448"
      d:DataContext="{d:DesignInstance viewModel:AutoCutRuningViewModel, IsDesignTimeCreatable=True}">
    <behaviors:Interaction.Triggers>
        <behaviors:EventTrigger EventName="Loaded">
            <behaviors:InvokeCommandAction 
                Command="{Binding InitCommand}" 
                CommandParameter="可选参数"/>
        </behaviors:EventTrigger>
    </behaviors:Interaction.Triggers>
    <UserControl.Resources>
        <Style TargetType="TextBlock">
            <Setter Property="FontSize" Value="18"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="HorizontalAlignment" Value="Right"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
        </Style>
        <Style x:Key="greenButton" TargetType="Button">
            <!-- 基本样式设置 -->
            <Setter Property="Background" Value="#39D11A"/>
            <!-- 主色 -->
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderBrush" Value="#388E3C"/>
            <!-- 边框色 -->
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="12 6"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <!-- 按钮视觉结构 -->
                        <Border x:Name="border" 
                        Background="{TemplateBinding Background}"
                        BorderBrush="{TemplateBinding BorderBrush}"
                        BorderThickness="{TemplateBinding BorderThickness}"
                        CornerRadius="4"
                        SnapsToDevicePixels="True">
                            <ContentPresenter x:Name="contentPresenter"
                                    Content="{TemplateBinding Content}"
                                    ContentTemplate="{TemplateBinding ContentTemplate}"
                                    HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                    VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                                    Margin="{TemplateBinding Padding}"/>
                        </Border>

                        <!-- 按钮的视觉状态 -->
                        <ControlTemplate.Triggers>
                            <!-- 鼠标悬停效果 -->
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#43A047"/>
                                <!-- 深一点的颜色 -->
                                <Setter Property="BorderBrush" Value="#2E7D32"/>
                            </Trigger>

                            <!-- 按下效果 -->
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Background" Value="#388E3C"/>
                                <Setter Property="BorderBrush" Value="#1B5E20"/>
                                <Setter TargetName="contentPresenter" Property="RenderTransform">
                                    <Setter.Value>
                                        <ScaleTransform ScaleX="0.98" ScaleY="0.98"/>
                                    </Setter.Value>
                                </Setter>
                            </Trigger>

                            <!-- 禁用状态 -->
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Background" Value="#BDBDBD"/>
                                <Setter Property="BorderBrush" Value="#9E9E9E"/>
                                <Setter Property="Foreground" Value="#757575"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style x:Key="redButton" TargetType="Button">
            <!-- 基本样式设置 -->
            <Setter Property="Background" Value="#FFFF6E3B"/>
            <!-- 主色 -->
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderBrush" Value="#FFFF6E3B"/>
            <!-- 边框色 -->
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="12 6"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <!-- 按钮视觉结构 -->
                        <Border x:Name="border" 
                        Background="{TemplateBinding Background}"
                        BorderBrush="{TemplateBinding BorderBrush}"
                        BorderThickness="{TemplateBinding BorderThickness}"
                        CornerRadius="4"
                        SnapsToDevicePixels="True">
                            <ContentPresenter x:Name="contentPresenter"
                                    Content="{TemplateBinding Content}"
                                    ContentTemplate="{TemplateBinding ContentTemplate}"
                                    HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                    VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                                    Margin="{TemplateBinding Padding}"/>
                        </Border>

                        <!-- 按钮的视觉状态 -->
                        <ControlTemplate.Triggers>
                            <!-- 鼠标悬停效果 -->
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#FFFF3D3B"/>
                                <!-- 深一点的颜色 -->
                                <Setter Property="BorderBrush" Value="#FFFF3D3B"/>
                            </Trigger>

                            <!-- 按下效果 -->
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Background" Value="#FFFF3D6B"/>
                                <Setter Property="BorderBrush" Value="#FFFF3D6B"/>
                                <Setter TargetName="contentPresenter" Property="RenderTransform">
                                    <Setter.Value>
                                        <ScaleTransform ScaleX="0.98" ScaleY="0.98"/>
                                    </Setter.Value>
                                </Setter>
                            </Trigger>

                            <!-- 禁用状态 -->
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Background" Value="#BDBDBD"/>
                                <Setter Property="BorderBrush" Value="#9E9E9E"/>
                                <Setter Property="Foreground" Value="#757575"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="50*" />
            <ColumnDefinition Width="50*" />
        </Grid.ColumnDefinitions>
        <Grid Grid.Column="0">
            <common:CameraCommon Width="765" Height="640" />
        </Grid>
        <Grid Grid.Column="1">
            <Grid.RowDefinitions>
                <RowDefinition Height="auto"/>
                <RowDefinition Height="auto"/>
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="3*"/>
                <ColumnDefinition Width="2*"/>
            </Grid.ColumnDefinitions>
            <Label Grid.ColumnSpan="2" Margin="30,30,0,0" Content="自动执行状态" FontSize="25" FontWeight="Bold" />
            <auto:AutoGrid Grid.Row="1" Margin="10" Columns="4*,4*,2*" RowCount="10" ChildMargin="5">
                <TextBlock Text="测高位置"/>
                <Controls:InputTextBox Text="{Binding AfterHeightMeasurementZ}"/>
                <TextBlock HorizontalAlignment="Left" Text="mm"/>
                <TextBlock Text="磨刀片高度"/>
                <Controls:InputTextBox Text="{Binding SharpenBladeHeight}"/>
                <TextBlock HorizontalAlignment="Left" Text="mm"/>
                <TextBlock Text="磨刀速度"/>
                <Controls:InputTextBox Text="{Binding SharpenSpeed}"/>
                <TextBlock HorizontalAlignment="Left" Text="mm/s"/>
                <TextBlock Text="磨刀进度"/>
                <Controls:InputTextBox Text="{Binding SharpenProgress}"/>
                <TextBlock HorizontalAlignment="Left" Text=""/>
                <TextBlock Text="总磨损量"/>
                <Controls:InputTextBox Text="{Binding TotalWearAmount}"/>
                <TextBlock HorizontalAlignment="Left" Text="mm"/>
                <TextBlock Text="型号参数No"/>
                <Controls:InputTextBox Text="{Binding DeviceDataNo}"/>
                <TextBlock HorizontalAlignment="Left" Text="ID"/>
                <TextBlock Text="切割刀片高度"/>
                <Controls:InputTextBox Text="{Binding CutBladeHeight}"/>
                <TextBlock HorizontalAlignment="Left" Text="mm"/>
                <TextBlock Text="切割速度"/>
                <Controls:InputTextBox Text="{Binding CutSpeed}"/>
                <TextBlock HorizontalAlignment="Left" Text="mm/s"/>
                <TextBlock Text="切割进度"/>
                <Controls:InputTextBox Text="{Binding CutProgress}"/>
                <TextBlock HorizontalAlignment="Left" Text=""/>
                <TextBlock Text="自更换刀片起"/>
                <Controls:InputTextBox Text="{Binding AfterReplaceBladeCutTimes}"/>
                <TextBlock HorizontalAlignment="Left" Text="刀"/>
            </auto:AutoGrid>
            <GroupBox Grid.Row="1" Grid.Column="1" materialDesign:ColorZoneAssist.Mode="SecondaryMid">
                <GroupBox.Resources>
                    <ResourceDictionary>
                        <ResourceDictionary.MergedDictionaries>
                            <ResourceDictionary Source="pack://application:,,,/MaterialDesignThemes.Wpf;component/Themes/MaterialDesign3.Defaults.xaml"/>
                        </ResourceDictionary.MergedDictionaries>
                    </ResourceDictionary>
                </GroupBox.Resources>
                <GroupBox.HeaderTemplate>
                    <DataTemplate>
                        <StackPanel Orientation="Horizontal">
                            <materialDesign:PackIcon Width="32" Height="32" VerticalAlignment="Center" Kind="InformationSlabBox" />
                            <TextBlock Margin="8,0,0,0" VerticalAlignment="Center" Style="{StaticResource MaterialDesignSubtitle1TextBlock}" FontSize="18" Text="实时信息" FontWeight="Bold"/>
                        </StackPanel>
                    </DataTemplate>
                </GroupBox.HeaderTemplate>
                <custom:ShowLastListBox ItemsSource="{Binding MessageList}" d:ItemsSource="{d:SampleData ItemCount=20}" Style="{StaticResource MaterialDesignListBox}" Height="300">
                    <custom:ShowLastListBox.ItemContainerStyle>
                        <Style TargetType="ListBoxItem" BasedOn="{StaticResource MaterialDesignListBoxItem}">
                            <Setter Property="Margin" Value="0,0,0,1"/>
                            <Setter Property="Padding" Value="0"/>
                            <Setter Property="materialDesign:ListBoxItemAssist.ShowSelection" Value="False"/>
                        </Style>
                    </custom:ShowLastListBox.ItemContainerStyle>
                    <custom:ShowLastListBox.ItemTemplate>
                        <DataTemplate>
                            <Border>
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition/>
                                    </Grid.ColumnDefinitions>
                                    <StackPanel>
                                        <TextBlock FontSize="10" Text="{Binding Time}" />
                                        <TextBlock FontSize="12" TextWrapping="Wrap" FontWeight="Bold" Text="{Binding Message}" d:Text="999999999999999999999999999999999999999999999"/>
                                    </StackPanel>
                                </Grid>
                            </Border>
                        </DataTemplate>
                    </custom:ShowLastListBox.ItemTemplate>
                </custom:ShowLastListBox>
            </GroupBox>
        </Grid>
    </Grid>
</UserControl>
