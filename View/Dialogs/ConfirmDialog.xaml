<UserControl x:Class="精密切割系统.View.Dialogs.ConfirmDialog"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:精密切割系统.View.Dialogs"
             xmlns:viewModel="clr-namespace:精密切割系统.ViewModel.Dialogs"
             xmlns:prism="http://prismlibrary.com/"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             d:DataContext="{d:DesignInstance viewModel:ConfirmDialogViewModel, IsDesignTimeCreatable=True}"
             mc:Ignorable="d" Height="100" Width="300" Background="White">
    <Grid>
        <Grid.Resources>
            <ResourceDictionary>
                <ResourceDictionary.MergedDictionaries>
                    <materialDesign:BundledTheme BaseTheme="Dark" PrimaryColor="LightGreen"  SecondaryColor="Lime" />
                    <ResourceDictionary Source="pack://application:,,,/MaterialDesignThemes.Wpf;component/Themes/MaterialDesign3.Defaults.xaml"/>
                </ResourceDictionary.MergedDictionaries>
            </ResourceDictionary>
        </Grid.Resources>
        <Button
             Height="100" Width="300"
            FontSize="25"
            FontWeight="Bold"
            materialDesign:ButtonProgressAssist.IsIndeterminate="True"
            materialDesign:ButtonProgressAssist.IsIndicatorVisible="True"
            materialDesign:ButtonProgressAssist.Value="-1"
            Content="{Binding ButtonContent}"
            d:Content="6666666"
            Command="{Binding ConfirmCommand}"
            Style="{StaticResource MaterialDesignFlatButton}" />
    </Grid>
</UserControl>
