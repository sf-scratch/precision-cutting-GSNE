<UserControl
    x:Class="精密切割系统.View.Dialogs.SelectDateTimeDialog"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:behaviors="http://schemas.microsoft.com/xaml/behaviors"
    xmlns:behvr="clr-namespace:精密切割系统.Behaviors"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:精密切割系统.View.Dialogs"
    xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <Viewbox Width="800" Stretch="Uniform">
            <StackPanel Orientation="Horizontal">
                <Calendar
                    x:Name="Calendar"
                    Margin="-1,-2,-1,-2"
                    SelectedDate="{Binding SelectedDate, RelativeSource={RelativeSource AncestorType=UserControl}}" />
                <materialDesign:Clock
                    x:Name="CombinedClock"
                    DisplayAutomation="CycleWithSeconds"
                    Is24Hours="True"
                    Time="{Binding SelectedDate, RelativeSource={RelativeSource AncestorType=UserControl}, Mode=OneWay}"
                    TimeChanged="CombinedClock_TimeChanged" />
            </StackPanel>
        </Viewbox>
        <StackPanel
            Grid.Row="1"
            Margin="8"
            HorizontalAlignment="Right"
            Orientation="Horizontal">
            <Button
                Command="{x:Static materialDesign:DialogHost.CloseDialogCommand}"
                CommandParameter="0"
                Content="取消"
                Style="{StaticResource MaterialDesignFlatButton}" />
            <Button
                Command="{x:Static materialDesign:DialogHost.CloseDialogCommand}"
                CommandParameter="{Binding SelectedDate, RelativeSource={RelativeSource AncestorType=UserControl}}"
                Content="确认"
                Style="{StaticResource MaterialDesignFlatButton}" />
        </StackPanel>
    </Grid>
</UserControl>